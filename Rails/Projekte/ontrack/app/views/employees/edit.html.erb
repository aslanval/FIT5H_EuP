<h1>Editing Employee</h1>

<%= render 'form', employee: @employee %>

<%= link_to 'Show', project_employee_path(@project.id, @employee.id) %> |
<%= link_to 'Back', project_employees_path(@project.id) %>
 